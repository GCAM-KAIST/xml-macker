<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:chrome="clr-namespace:XMLMacker.Chrome">

    <!--
        Default style for the liquid-glass container (GlassPanel). 1:1 port of GlassPanel.swift.
        Layer order (bottom → top):
          1. tint base     , the translucent panel fill (XM.panel, theme-driven)
          2. colored wash   , optional per-panel tint (cool/violet/amber/danger) via TintBrush
          3. top gloss      , white α0.14 → 0 vertical gradient over the top 18% (inset highlight)
          4. content        , the hosted ContentPresenter
          5. hairline border, 0.5px XM.hairlineS, snapped to device pixels
        There is no live blur behind a child element in WPF (window-level acrylic comes from
        Shared/Acrylic); this reproduces the static composition.
    -->
    <Style x:Key="XM.GlassPanel" TargetType="{x:Type chrome:GlassPanel}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type chrome:GlassPanel}">
                    <Grid>
                        <!-- 1. translucent panel fill -->
                        <Border CornerRadius="{TemplateBinding CornerRadius}"
                                Background="{DynamicResource XM.panel}"
                                SnapsToDevicePixels="True" />

                        <!-- 2. optional colored wash -->
                        <Border CornerRadius="{TemplateBinding CornerRadius}"
                                Background="{TemplateBinding TintBrush}"
                                SnapsToDevicePixels="True" />

                        <!-- 3. top gloss (inset highlight) -->
                        <Border CornerRadius="{TemplateBinding CornerRadius}"
                                SnapsToDevicePixels="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Offset="0.0"  Color="#24FFFFFF" />
                                    <GradientStop Offset="0.18" Color="#00FFFFFF" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- 4. content -->
                        <Border CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}"
                                ClipToBounds="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>

                        <!-- 5. hairline border -->
                        <Border CornerRadius="{TemplateBinding CornerRadius}"
                                BorderBrush="{DynamicResource XM.hairlineS}"
                                BorderThickness="0.5"
                                SnapsToDevicePixels="True" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
