<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Shared "Fix" button cell used in error tables (Validation window, Find/Preview error lists).
        DataContext for this template is a ParseError-shaped row exposing a nullable Fix (FixIt?)
        with a Title. The cell is hidden when Fix == null and its tooltip is Fix.Title.

        The hosting control wires clicks by attaching a handler for Button.Click on the list/grid
        (this dictionary is x:Class-free, so it carries no code-behind of its own).
    -->
    <DataTemplate x:Key="XM.FixCellTemplate">
        <Button Content="Fix"
                Cursor="Hand"
                ToolTip="{Binding Fix.Title}"
                Tag="{Binding Fix}"
                Focusable="False">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            CornerRadius="5"
                            Padding="8,2"
                            Background="{DynamicResource XM.accent}"
                            BorderBrush="{DynamicResource XM.hairlineS}"
                            BorderThickness="0.5">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.70" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Foreground" Value="{DynamicResource XM.bg}" />
                    <Setter Property="FontSize" Value="11" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Fix}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </DataTemplate>

</ResourceDictionary>
